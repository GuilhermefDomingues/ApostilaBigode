const nome = prompt("Digite seu nome:");
const sobrenome = prompt("Digite seu sobrenome:");

const nomeCompleto = `${nome} ${sobrenome}`;

console.log(`Meu primeiro nome é ${nome} e sobrenome é ${sobrenome}. Nome completo é ${nomeCompleto}.`);